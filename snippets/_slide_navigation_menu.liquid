{% comment %} 幻灯片导航菜单 - 左上角目录按钮 {% endcomment %}
<div
  class="fixed top-6 left-8 z-50 group"
  @mouseenter="menuOpen = true"
  @mouseleave="menuOpen = false"
>
  <div
    class="flex items-center gap-2 bg-white/90 backdrop-blur-sm shadow-md border border-slate-200 px-4 py-2 rounded-full cursor-pointer hover:shadow-lg transition-all text-slate-700 hover:text-primary"
  >
    <i class="fas fa-th-large w-5 h-5"></i>
    <span class="font-bold text-sm">目录</span>
  </div>

  <div
    x-show="menuOpen"
    x-transition:enter="transition ease-out duration-200"
    x-transition:enter-start="opacity-0 translate-y-2"
    x-transition:enter-end="opacity-100 translate-y-0"
    x-transition:leave="transition ease-in duration-150"
    x-transition:leave-start="opacity-100 translate-y-0"
    x-transition:leave-end="opacity-0 translate-y-2"
    class="absolute top-full left-0 pt-4 w-[400px]"
    style="display: none"
  >
    <div
      class="bg-white/95 backdrop-blur-md rounded-xl shadow-2xl border border-slate-100 p-4 max-h-[70vh] overflow-y-auto no-scrollbar"
    >
      <div class="grid grid-cols-2 gap-2">
        {% for slide in slides %}
          <button
            @click="goToSlide({{ forloop.index0 }})"
            class="text-left px-3 py-2 rounded-lg text-xs font-medium transition-colors flex items-center gap-2 w-full"
            :class="currentIndex === {{ forloop.index0 }} ? 'bg-primary text-white shadow-sm' : 'text-slate-600 hover:bg-slate-100'"
          >
            <span
              class="w-5 h-5 flex items-center justify-center rounded-full text-[10px] shrink-0"
              :class="currentIndex === {{ forloop.index0 }} ? 'bg-white/20' : 'bg-slate-200 text-slate-500'"
            >{{ forloop.index }}</span>

            <span class="truncate">{{ slide.settings.title | default: slide.title }}</span>
          </button>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
